# `hybrid`

## 1. 제가 선택한 알고리즘

제가 이번 프로젝트에서 선택한 알고리즘은 `hybrid` 추천기입니다. 이 알고리즘은 하나의 기준만으로 추천하는 방식이 아니라, 여러 점수를 함께 반영하는 경량 하이브리드 구조입니다. 크게 보면 세 가지 요소를 사용했습니다. 첫 번째는 기준 곡과 후보 곡의 오디오 특성 유사도를 보는 콘텐츠 점수이고, 두 번째는 사용자가 좋아요한 곡들과 후보 곡이 얼마나 비슷한지를 보는 사용자 선호 점수이며, 세 번째는 `play`, `like`, `skip`, `unlike` 같은 로그를 반영하는 행동 보정 점수입니다. 정리하면 단순히 비슷한 음악만 찾는 것이 아니라, 비슷하면서도 사용자가 더 좋아할 가능성이 높은 음악을 추천하는 방향으로 설계했습니다.

## 2. 왜 `hybrid`를 선택했는가

프로젝트 기본 알고리즘인 `faiss_cbf`는 오디오 유사도 기반 추천이라는 점에서 좋은 베이스라인입니다. 구현이 단순하고 빠르게 동작하며, 곡 하나만 선택해도 바로 비슷한 음악을 추천할 수 있다는 장점이 있습니다. 하지만 이 방식만으로는 사용자 개인 취향이 직접 반영되지 않고, 좋아요나 스킵 같은 실제 피드백도 충분히 활용하기 어렵습니다. 같은 기준 곡을 선택하더라도 사용자마다 다른 결과가 나와야 더 개인화된 추천이라고 볼 수 있는데, `faiss_cbf`는 이 부분이 약하다고 판단했습니다.

그래서 저는 `faiss_cbf`를 완전히 버리기보다는, 그 위에 사용자 선호와 행동 로그를 추가한 `hybrid` 구조를 선택했습니다. 발표 관점에서도 이 선택이 자연스럽다고 생각했습니다. 먼저 `faiss_cbf`를 베이스라인으로 설명하고, 그다음 `hybrid`를 확장 모델로 소개하면 흐름이 분명해지기 때문입니다. 즉, 기본 오디오 유사도 추천의 장점은 유지하면서 개인화 요소를 더한 것이 이번 선택의 핵심입니다.

## 3. 어떻게 구현했는가

구현 파일은 `algorithms/hybrid_recommender.py`입니다. 핵심 아이디어는 콘텐츠 점수, 사용자 점수, 행동 보정 점수를 가중합으로 결합하는 방식입니다.

```python
final_score = 0.6 * content_score + 0.3 * user_score + 0.1 * behavior_score
```

이 구조에서는 콘텐츠 점수를 가장 크게 반영하고, 사용자 좋아요 이력은 중간 정도, 행동 로그는 보조적으로 반영하도록 잡았습니다. 이유는 현재 프로젝트의 가장 안정적인 추천 신호가 오디오 유사도라고 판단했기 때문입니다. 좋아요나 로그 데이터는 의미가 있지만, 데이터 양이 충분하지 않은 상황에서 너무 큰 비중을 주면 오히려 결과가 불안정해질 수 있다고 봤습니다.

곡 기반 추천에서는 기준 곡을 하나 받아 FAISS로 비슷한 후보 곡을 찾고, 각 후보에 대해 콘텐츠 점수와 행동 보정 점수를 계산해 최종 점수를 만듭니다. 유저 기반 추천에서는 사용자의 좋아요 곡 목록을 읽고, 그 곡들에서 후보를 모은 뒤 사용자의 좋아요 이력과 얼마나 비슷한지를 다시 계산해 점수에 반영합니다. 마지막에는 이미 좋아요한 곡은 제외하고 추천하도록 구성했습니다.

## 4. `faiss_cbf`와 차이점

`faiss_cbf`는 오디오 유사도만 사용하는 알고리즘입니다. 쉽게 말하면 "이 곡과 비슷한 곡"을 찾는 베이스라인 모델입니다. 반면 `hybrid`는 오디오 유사도뿐 아니라 사용자 좋아요와 행동 로그까지 함께 사용합니다. 따라서 "이 곡과 비슷하면서도 사용자가 좋아할 가능성이 높은 곡"을 추천하는 구조라고 설명할 수 있습니다.

즉 두 알고리즘의 가장 큰 차이는 개인화 여부입니다. `faiss_cbf`는 곡 중심의 유사도 추천이고, `hybrid`는 그 위에 사용자 정보를 더해 개인화 추천으로 확장한 모델이라고 보시면 됩니다.

## 5. 추가로 반영한 부분

UI에서도 추천 경험을 조금 더 자연스럽게 만들기 위해 일부를 보완했습니다. 카드의 `x` 버튼을 누르면 단순히 흐리게만 처리하는 것이 아니라 `skip` 로그가 저장되도록 했고, 현재 추천 목록에서 해당 곡을 제외하도록 수정했습니다. 또 같은 곡이 바로 다시 추천되지 않도록 제외할 곡 목록을 서버에 함께 전달하는 방식으로 보완했습니다. 이 부분은 알고리즘 자체뿐 아니라 추천 시스템의 사용자 경험까지 고려했다는 점에서 발표 포인트로 활용할 수 있습니다.

## 6. 한 줄 요약

저는 오디오 특성 기반 FAISS 추천기를 베이스라인으로 두고, 여기에 사용자 좋아요 이력과 행동 로그를 결합한 경량 하이브리드 추천기를 구현해 개인화 추천 성능을 강화하고자 했습니다.