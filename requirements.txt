# YouTube API
google-api-python-client>=2.100.0
isodate>=0.6.1

# 오디오 수집 + 분석
yt-dlp>=2024.1.0
librosa==0.10.2.post1
soundfile>=0.12.1

# 데이터 처리
numpy==1.26.4
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm>=4.66.0
beautifulsoup4>=4.12.0
requests>=2.31.0

# librosa dependency pinning (avoid llvmlite build failures)
numba==0.59.1
llvmlite==0.42.0

# LangChain / LangGraph
langchain>=0.1.0
langchain-google-genai>=1.0.0
langgraph>=0.0.30

# 벡터 검색
faiss-cpu>=1.7.4

# 딥러닝 (RL / BERT 선택자)
torch>=2.0.0
transformers>=4.35.0
sentence-transformers>=2.2.0

# 협업 필터링 (SVD 선택자)
# NOTE: scikit-surprise is not reliably buildable on some macOS environments.
# Install manually only when needed:
#   pip install scikit-surprise==1.1.4

# 백엔드
fastapi>=0.104.0
uvicorn>=0.24.0
python-dotenv>=1.0.0
aiofiles>=23.0.0
bcrypt>=4.1.0
PyJWT>=2.8.0
